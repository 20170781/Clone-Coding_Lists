type Movie {
  id: String!
  name: String!
  score: Float!
}

type Query {
  movieList: [Movie]!
  movie(id: String!): Movie
}

type Mutation {
  addMovie(name: String!, score: Float!): Movie!
  deleteMovie(id: String!): Boolean!
}
